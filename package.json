{
  "name": "weather-feelslike-site",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20260524.0",
    "vite": "^5.4.11",
    "typescript": "^5.6.3"
  },
  "devDependencies": {}
}
